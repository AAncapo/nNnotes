type Languages = "es" | "en";

const TEXT = {
  settings: ["Configuración", "Settings"],
  notLoggedIn: [
    "Estas usando nNnotes como invitado",
    "You're using nNnotes as a guest",
  ],
  signInButton: ["Inicia Sesión / Regístrate", "Sign In / Register"],
};

// const localize = (lang?: Languages) => {
//     return !lang?TEXT
// };

// export default localize;
